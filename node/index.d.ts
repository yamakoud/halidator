import { Validator } from '../core/validator';
import { ValidatorOptions } from '../core/types';
export declare class Halidator extends Validator {
    constructor(options?: ValidatorOptions);
}
export default Halidator;
